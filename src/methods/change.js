/**
 * @function m_change
 * 
 * @param { { warn: boolean } } config 配置对象
 * @param { { method: string, key: string, value: any } } changes 变更对象
 * @param { Function } callback 回调函数
 * 
 * @returns { void }
 */
export function m_change(config, changes, callback) {
    const { method, key, value } = changes;
}